machine:
  services:
    - docker

test:
  override:
    - docker build 6.2.1
    - docker build 6.2
    - docker build 6.1.1
    - docker build 6.1
    - docker build 6.0.1
    - docker build 6.0
    - docker build 5.9.3
